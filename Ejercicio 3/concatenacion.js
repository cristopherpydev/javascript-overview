// 3. Concatenación de Cadenas
// Título: Generador de Mensajes
// Crea las variables "nombre", "edad" y "ciudad".
// Crea un mensaje (string) que combine estas variables en una frase completa usando la
// concatenación o template literals (backticks ` `).
// Muestra el mensaje en la consola.

let nombre = 'Cristopher';
let edad = 25;
let ciudad = 'Icod de los Vinos';

let frase = 'Soy ' + nombre + ' tengo ' + edad + ' años y vivo en ' + ciudad + '.'; 
console.log(frase);